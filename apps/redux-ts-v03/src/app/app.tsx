// eslint-disable-next-line @typescript-eslint/no-unused-vars
import styles from './app.module.css';
import React from "react";
import {Typography} from "@mui/material";

const App: React.FC = () => {
  return (
    <div>
      <Typography variant="h6">Redux in TS</Typography>
    </div>
  );
}

export default App;
